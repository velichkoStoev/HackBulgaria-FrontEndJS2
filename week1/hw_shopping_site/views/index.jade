extends layout
block content
    each product, id in products
        .product-info
            h2.product-title: a(href="/" + id   )= product.title
            img(src=product.cover)
            p.product-price= product.price